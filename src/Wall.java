/**
 * 
 * Wall cell (no pass)
 *
 */
public class Wall extends Cell {

   /**
    * Construct Wall object
    * @param row - map row
    * @param col - map column
    */
   public Wall(int row, int col) {
      super(row, col);
   }
}
